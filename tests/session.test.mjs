import test from"node:test";import assert from"node:assert/strict";import{initialSession,sessionReducer}from"../src/domain/session.mjs";test("session transitions are explicit",()=>{const running=sessionReducer(initialSession,{type:"start",at:1});assert.equal(sessionReducer(running,{type:"tick"}).elapsedSeconds,1);assert.equal(sessionReducer(running,{type:"pause"}).status,"paused")});
