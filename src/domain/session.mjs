export const initialSession={status:"idle",startedAt:null,elapsedSeconds:0};export function sessionReducer(state,action){switch(action.type){case"start":return{status:"running",startedAt:action.at,elapsedSeconds:0};case"tick":return state.status==="running"?{...state,elapsedSeconds:state.elapsedSeconds+1}:state;case"pause":return{...state,status:"paused"};case"reset":return initialSession;default:return state;}}
